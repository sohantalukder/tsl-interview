enum routes {
  /**
   * @param {string} routes
   * @returns {string}
   */
  splash = 'SPLASH',
  login = 'LOGIN',
  loginPassword = 'LOGIN_PASSWORD',
  profile = 'PROFILE',
  home = 'HOME',
  favorites = 'FAVORITES',
  mapView = 'MAP_VIEW',
  productDetail = 'PRODUCT_DETAIL',
  bottomTab = 'BOTTOM_TAB',
}

export default routes;
